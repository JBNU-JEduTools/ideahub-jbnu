let Post,mongoose,Joi,sanitizeHtml;_33b‍.x([["getPostById",()=>getPostById],["checkOwnPost",()=>checkOwnPost],["write",()=>write],["list",()=>list],["read",()=>read],["remove",()=>remove],["update",()=>update]]);_33b‍.w("../../models/post",[["default",["Post"],function(v){Post=v}]]);_33b‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_33b‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_33b‍.w("sanitize-html",[["default",["sanitizeHtml"],function(v){sanitizeHtml=v}]]);/*controlls posting middleware functions*/






const { ObjectId } = mongoose.Types;

       const getPostById = async (ctx, next) => {
  const { id } = ctx.params;
  if (!ObjectId.isValid(id)) {
    ctx.status = 400;
    return;
  }
  try {
    const post = await Post.findById(id);
    if (!post) {
      ctx.status = 404;
      return;
    }
    ctx.state.post = post;
    return next();
  } catch (e) {
    ctx.throw(500, e);
  }
};

       const checkOwnPost = (ctx, next) => {
  const { user, post } = ctx.state;
  //mongoDB에서 조회한 데이터의 id값을 문자열과 비교하기 위해 toString 사용
  if (post.user._id.toString() !== user._id) {
    ctx.status = 403;
    return;
  }
  return next();
};

       const write = async ctx => {
  const schema = Joi.object().keys({
    title: Joi.string().required(),
    category: Joi.string().required(),
    status: Joi.string().required(),
    date: Joi.string().required(),
    place: Joi.string().required(),
    description: Joi.string().required(),
  });

  const result = Joi.validate(ctx.request.body, schema);
  if (result.error) {
    ctx.status = 400;
    ctx.body = result.error;
    return;
  }

  const {
    title,
    category,
    status,
    date,
    place,
    description,
  } = ctx.request.body;

  const post = new Post({
    title,
    category,
    status,
    date,
    place,
    description,
    user: ctx.state.user,
  });
  try {
    await post.save();
    ctx.body = post;
  } catch (e) {
    ctx.throw(500, e);
  }
};

//removes html tags, slices paragraph.
const removeHtmlAndShorten = body => {
  const filtered = sanitizeHtml(body, {
    allowedTags: [],
    //   'h1',
    //   'h2',
    //   'b',
    //   'i',
    //   'u',
    //   's',
    //   'p',
    //   'ul',
    //   'ol',
    //   'li',
    //   'blockquote',
    //   'a',
    //   'img',
    // ],
    // allowedAttributes: {
    //   a: ['href', 'name', 'target'],
    //   img: ['src'],
    //   li: ['class'],
    // },
    // allowedSchemes: ['data', 'http'],
  });
  return filtered.length < 300 ? filtered : `${filtered.slice(0, 300)}...`;
};

//������ ��ȸ
       const list = async ctx => {
  //http://localhost:4000/api/posts?page=2 �� �������� �����Ͽ� ��ȸ
  //page �������� ������ �� ���� int�� �Ľ��ϰ�, ������ 1�� �Ľ�
  const page = parseInt(ctx.query.page || '1', 5);

  if (page < 1) {
    ctx.status = 400;
    return;
  }

  const { username } = ctx.query;
  //??
  const query = {
    //username, ���� ��ȿ�ϸ� ��ü �ȿ� �ְ�, �ƴϸ� ���� ����
    ...(username ? { 'user.username': username } : {}),
  };

  try {
    const posts = await Post.find(query)
      .sort({ _id: -1 })
      .limit(5)
      .skip((page - 1) * 5)
      .exec();
    const postCount = await Post.countDocuments().exec();
    ctx.set('Last-Page', Math.ceil(postCount / 5));
    ctx.body = posts
      .map(post => post.toJSON())
      .map(post => ({
        ...post,
        description: removeHtmlAndShorten(post.description),
      }));
  } catch (e) {
    ctx.throw(500, e);
  }
};

//getPostById에서 ctx.state.post에 포스트 정보를 넣어주므로.
       const read = ctx => {
  ctx.body = ctx.state.post;
};

       const remove = async ctx => {
  const { id } = ctx.params;
  try {
    await Post.findByIdAndRemove(id).exec();
    ctx.status = 204;
  } catch (e) {
    ctx.throw(500, e);
  }
};
       const update = async ctx => {
  const schema = Joi.object().keys({
    title: Joi.string(),
    category: Joi.string(),
    status: Joi.string(),
    date: Joi.string(),
    place: Joi.string(),
    description: Joi.string(),
    prized: Joi.array(),
  });

  const result = Joi.validate(ctx.request.body, schema);
  if (result.error) {
    ctx.status = 400;
    ctx.body = result.error;
    return;
  }

  const { id } = ctx.params;
  try {
    const post = Post.findByIdAndUpdate(id, ctx.request.body, {
      new: true,
    }).exec();
    if (!post) {
      ctx.status = 404;
      return;
    }
    ctx.body = post;
  } catch (e) {
    ctx.throw(500, e);
  }
};
